export class MessageConverter {
    constructor() {
    }

    public convertViberMessage(message: any): any {
        const convertedMessage: any = {};
        return convertedMessage;
    }

    public convertTelegramMessage(message: any): any {
        const convertedMessage: any = {};
        return convertedMessage;

    }

    public convertToViberMessage(message: any): any {
        const convertedMessage: any = {};
        return convertedMessage;
    }

    public convertToTelegramMessage(message: any): any {
        const convertedMessage: any = {};
        return convertedMessage;
    }
}
